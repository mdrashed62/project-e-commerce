import React from 'react';

const page = ({params}) => {
    console.log(params.id)

    
    return (
        <div>
           <h1>Here will be show single product info</h1>
        </div>
    );
};

export default page;